version: "3.9"

services:
  app:
    build: .
    container_name: notes-app
    ports:
      - "8080:8000"
    environment:
      HOST_PORT: 8080
    volumes:
      - ./database:/app/database
      - ./.env:/app/.env
